<configuration>

    <!-- Define err console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- Define the log output format -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Set the default logging level and add the STDOUT appender -->
    <root level="WARN">
        <appender-ref ref="STDOUT"/>
    </root>

    <!-- Set log level for your project to DEBUG -->
    <logger name="concurentDocs" level="DEBUG"/>

    <!-- Set log level for Cassandra driver to WARN -->
    <logger name="com.datastax.oss.driver" level="WARN"/>

</configuration>
